name: TDLib
cflags: ""
ldflags: "-L/usr/local/lib -ltdjson_static -ltdjson_private -ltdclient -ltdcore -ltdactor -ltddb -ltdsqlite -ltdnet -ltdutils -lstdc++ -lssl -lcrypto -ldl -lz -lm"
packages: ""
destdir: src/tdlib/
definitions:
  tdlib:
    description: Telegram MTProto library
    includes:
      - td/telegram/td_log.h
      - td/telegram/td_json_client.h
    prefixes: [ "td" ]
options:
  import_docstrings: full